# Download builds

android apk goes here for now:

```
public/downloads/nascent-motorsport.apk
```

download page (`/download`) links to `/downloads/nascent-motorsport.apk`,
so the file must keep this exact name (or update `APK_URL` in
`components/DownloadPage.jsx`).

Only distribute builds signed by the Nascent build pipeline.

:)